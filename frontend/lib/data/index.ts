/**
 * Data Module
 *
 * Centralized barrel export for data-related utilities and fixtures.
 * This module provides:
 * - Dashboard data and mock data (dashboard-data.ts)
 *
 * Usage:
 * ```typescript
 * import { dashboardStats, mockEmployees } from '@/lib/data';
 * ```
 */

// Dashboard statistics and mock data
export * from '../dashboard-data';
